# Implement str_len(value) without using len(). Count the characters manually 
# and return the number of characters in the string.

def str_len(value):
    val1 = 0
    for val2 in value:
        val1 = val1 +1
    return val1
